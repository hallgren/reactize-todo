<div>
  <% if @show_footer_and_toggle_all %>
    <footer id="footer">
      <span id="todo-count"><strong><%= @active_count %></strong> item left</span>
      <ul id="filters">
        <li>
          <a class="route <%= 'selected' unless @route %>" href="/">All</a>
        </li>
        <li>
          <a class="route <%= 'selected' if @route == 'active'%>" href="/active">Active</a>
        </li>
        <li>
          <a class="route <%= 'selected' if @route == 'completed'%>"  href="/completed">Completed</a>
        </li>
      </ul>
      <form action="/clear_completed" method="post">
        <button id="clear-completed">Clear completed (<%= @completed_count %>)</button>
      </form>
    </footer>
  <% end %>
</div>
